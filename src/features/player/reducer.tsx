const initialState = {
    position: [0,0],
    gameState: 'East Turn',
}

const playerReducer = (state=initialState) => {
    switch(true) {
        default: 
            return state;
    }
}

export default playerReducer;