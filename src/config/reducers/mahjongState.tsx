const initialState = {
    playerTurn: 'EAST',
}

const mahjongStateReducer = (state = initialState) => {
    const newState = {...state};
    switch(true){
        default:
            return 1; 
    }
}

export default mahjongStateReducer;